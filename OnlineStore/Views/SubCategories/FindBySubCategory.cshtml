@using OnlineStore.Models
@model SubCategoryModel

<div><h1>@Model.SubCategoryName</h1></div>
<table class="table">
    <tr class="firstRow">
        <th>Name</th>
        <th>Price</th>
        <th>View</th>
    </tr>
    @foreach(var product in Model.Products)
    {
        <tr>
            <td>@product.Name</td>
            <td>@product.Price</td>
            <td><a asp-area="" asp-contoller="Products" asp-controller="Products" asp-action="FindById" asp-route-specific="@product.ProductId">View</a></td>
        </tr>
    }
    <tr>
        <td><a class="btn btn-dark" asp-area="" asp-controller="Categories" asp-action="Categories">Back</a></td>
    </tr>

</table>